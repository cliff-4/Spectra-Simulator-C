{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-specsim",
            "type": "shell",
            "command": "cmake . && make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}